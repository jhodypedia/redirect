<%- include('layout', { 
  body: `
    <div class="mb-8">
      <h1 class="text-2xl font-bold text-gray-900">Ikhtisar Kampanye</h1>
      <p class="text-sm text-gray-500 mt-1">Pantau performa link affiliate dan trafik secara real-time.</p>
    </div>

    <div class="grid grid-cols-1 gap-5 sm:grid-cols-3 mb-8">
      <div class="bg-white overflow-hidden shadow-sm rounded-xl border border-gray-100">
        <div class="px-4 py-5 sm:p-6">
          <dt class="text-sm font-medium text-gray-500 truncate">Total Link Aktif</dt>
          <dd class="mt-1 text-3xl font-semibold text-gray-900">${stats.active} / ${stats.total}</dd>
        </div>
      </div>
      <div class="bg-white overflow-hidden shadow-sm rounded-xl border border-gray-100">
        <div class="px-4 py-5 sm:p-6">
          <dt class="text-sm font-medium text-gray-500 truncate">Total Klik</dt>
          <dd class="mt-1 text-3xl font-semibold text-blue-600">--</dd>
        </div>
      </div>
      <div class="bg-white overflow-hidden shadow-sm rounded-xl border border-gray-100">
        <div class="px-4 py-5 sm:p-6">
          <dt class="text-sm font-medium text-gray-500 truncate">Bot Diblokir</dt>
          <dd class="mt-1 text-3xl font-semibold text-red-500">--</dd>
        </div>
      </div>
    </div>

    <div class="bg-white shadow-sm rounded-xl border border-gray-200 overflow-hidden">
      <div class="px-4 py-5 border-b border-gray-200 sm:px-6 flex justify-between items-center">
        <h3 class="text-lg leading-6 font-medium text-gray-900">Daftar Link Redirect</h3>
      </div>
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200">
          <thead class="bg-gray-50">
            <tr>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Judul / Slug</th>
              <th scope="col" class="px-6 py-3 text-left text-xs
